# These are executed on Linux. CRLF endings would break the shebang and
# systemd parsing regardless of the checkout machine's autocrlf setting.
*.sh text eol=lf
deploy/Caddyfile text eol=lf
deploy/*.service text eol=lf
